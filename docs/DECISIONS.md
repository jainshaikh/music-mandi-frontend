# Architectural Decision Log

Lightweight decision log. Do not create speculative decisions for requirements not yet discussed.

---

**DEC-001: Use Next.js App Router**

- Date: 2026-09-20
- Decision: Use Next.js with the App Router (not Pages Router).
- Reason: Approved stack; supports the route-group architecture needed for isolated Artist/Advertiser/Admin dashboards.
- Status: Accepted
- Consequence: All routing uses `src/app` route groups; no `pages/` directory.

**DEC-002: Use TypeScript**

- Date: 2026-09-20
- Decision: Use TypeScript in strict mode for all frontend code.
- Reason: Approved stack; type safety across a multi-dashboard app.
- Status: Accepted
- Consequence: No `any` as a default escape hatch; domain types added once backend contracts are known.

**DEC-003: Use Tailwind CSS**

- Date: 2026-09-20
- Decision: Use Tailwind CSS for all new modern styling.
- Reason: Approved stack; pairs with shadcn/ui.
- Status: Accepted
- Consequence: No large standalone CSS systems for modern pages; legacy CSS stays isolated.

**DEC-004: Use shadcn/ui**

- Date: 2026-09-20
- Decision: Use shadcn/ui as the component foundation for modern pages.
- Reason: Approved stack; avoids a heavier component library like Material UI.
- Status: Accepted
- Consequence: New UI primitives are added via the shadcn CLI into `src/components/ui/`.

**DEC-005: Use pnpm**

- Date: 2026-09-20
- Decision: Use pnpm as the single package manager.
- Reason: Approved stack; avoids mixed lockfiles.
- Status: Accepted
- Consequence: Only `pnpm-lock.yaml` is committed; npm/yarn/bun lockfiles are not generated.

**DEC-006: Keep backend/database outside frontend scope**

- Date: 2026-09-20
- Decision: The frontend repository never contains database packages, schemas, or backend framework code.
- Reason: A separate backend team owns data and business logic; the frontend must remain independent of their implementation choice.
- Status: Accepted
- Consequence: No MongoDB/Prisma/Mongoose packages; API contracts are integrated, not invented.

**DEC-007: Isolate legacy code under public/legacy**

- Date: 2026-09-20
- Decision: All existing HTML/CSS/JS pages from the client's current site live under `public/legacy/`, isolated from modern styling/JS.
- Reason: Allows incremental migration without risking the working legacy site.
- Status: Accepted
- Consequence: See `docs/LEGACY_MIGRATION.md` for isolation rules.

**DEC-008: Use feature-based frontend organization**

- Date: 2026-09-20
- Decision: Organize business logic under `src/features/<domain>/` rather than one global `components` directory.
- Reason: Keeps three dashboards and shared code maintainable as the app grows.
- Status: Accepted
- Consequence: `src/app` stays limited to routes/layouts/composition.

**DEC-009: Do not install state/query libraries until required**

- Date: 2026-09-20
- Decision: Zustand and TanStack Query are approved but not installed during bootstrap.
- Reason: Avoid unused dependencies and premature architecture; native `fetch` and React state cover bootstrap needs.
- Status: Accepted
- Consequence: These are added only when a real feature (e.g. global music player, complex caching) requires them, and logged as a new entry in `docs/DEPENDENCIES.md`.

**DEC-010: Migrate music_mandi-website pages with per-component CSS Modules, not one global stylesheet**

- Date: 2026-09-20
- Decision: Public pages migrated from `music_mandi-website` get a co-located `Component.module.css` per component. Genuinely cross-component utility classes (`.btn`, `.eyebrow`, `.serif`) live in one plain `shared.css` scoped under `.mm-public`, rather than as CSS Modules — compound selectors like `.tone .btn` need `.btn` to resolve to the same global class everywhere, which per-file CSS Module hashing breaks.
- Reason: The source `globals.css` (~9,300 lines) was organized chronologically by patch/hotfix session, not by page, and was the exact "really big CSS that's mostly unused per page" problem being solved. A naive line-range split would have duplicated or dropped rules.
- Status: Accepted
- Consequence: Legacy design tokens/resets from the source's `:root`/`body` rules live in `src/app/(public)/public-base.css`, rescoped to `body.mm-public-body` (toggled only while a public route is mounted via `PublicScope.tsx`) so they can't leak into artist/advertiser/admin pages sharing the same `<body>`. See `docs/LEGACY_MIGRATION.md`.

**DEC-011: CSS Modules scopes ID selectors too — every ID-based rule needs `:global()`**

- Date: 2026-09-21
- Decision: Any migrated CSS Module rule targeting a literal HTML `id` (e.g. `#cursor`, `#ring`) must be wrapped as `:global(#id)`.
- Reason: Discovered as a real bug (custom cursor invisible in production) — Next.js's CSS Modules processor (Lightning CSS, via Turbopack) scopes/hashes ID selectors the same way it scopes class selectors, unlike the older webpack `css-loader`. An un-wrapped `#cursor { ... }` in a `.module.css` file compiles to `#SiteChrome-module__xyz__cursor`, which never matches a literal `id="cursor"` set in JSX.
- Status: Accepted
- Consequence: When migrating a new page/component, grep its `.module.css` for `#` selectors before wiring it up, not just `.class` ones.

**DEC-012: A component's raw DOM class/id hooks (closest(), querySelector) must use literal, non-module markers**

- Date: 2026-09-21
- Decision: When a migrated component's JS reads DOM structure via `closest()`/`querySelector()` with a literal class/id string (not through the `styles` object), the target element must carry a matching **literal, non-CSS-Module** class (e.g. `cursor-hover-target`) or id, added specifically for that purpose.
- Reason: Discovered as a real bug — `SiteChrome`'s custom-cursor grow effect matched `.feature`/`.dashboard` via `closest('a,button,.feature,.dashboard')`, a plain DOM string check entirely outside the CSS Modules system. Once `DashboardMock`/`SystemsCarousel` were converted to CSS Modules, those literal class names stopped existing in the DOM, and the check silently stopped matching — cursor tracking kept working, but the "grow near interactive things" feedback broke with no error anywhere.
- Status: Accepted
- Consequence: Before converting a component to CSS Modules, grep the rest of the migrated tree (and the component itself) for `closest(`/`querySelector(` calls referencing its class names, and add a literal marker class/id if any are found.

**DEC-013: Promote a bare utility class to shared.css reactively, the moment a 2nd migrated page uses it**

- Date: 2026-09-21
- Decision: A bare (non-compound) class used by only one migrated page/component stays in that file's own CSS Module. The moment a second migrated page/component also uses it as a bare class, move it into `shared.css` (scoped under `.mm-public`) and update every consumer to reference the plain string instead of `styles[...]`.
- Reason: `route-kicker` and `page-shell` (home → tele-ads) and `brand-gradient` (tele-ads → about) all hit this exact pattern: left as CSS Modules, the same conceptual class gets a different hash per file, so it renders fine standing alone but silently fails to apply wherever a compound selector in another file's source CSS overrides it (e.g. `.rights .eyebrow`). Promoting eagerly for every plausible-sounding name would be speculative; promoting reactively, only once a real second usage appears, keeps `shared.css` exactly as large as it needs to be.
- Status: Accepted
- Consequence: When migrating a new page, check each of its bare utility classes against `shared.css` and every already-migrated page's own CSS Module before deciding where it belongs.

**DEC-014: Disable convenience third-party-API features rather than leave them half-wired**

- Date: 2026-09-21
- Decision: When a migrated component's only purpose for a feature is a _convenience_ on top of an otherwise-complete flow (e.g. `ArtistNameSearchField`'s Spotify autocomplete, whose own helper text already says "just type your name — that's fine too"), and that feature depends on a backend route not in this frontend's scope, remove the dependent code path entirely rather than leaving a dead `fetch()` or an unreachable branch.
- Reason: A half-wired feature (event handlers and state for a search that can never return results) is harder to reason about than either a fully working feature or its clean absence, and risks looking like a bug rather than a scope boundary.
- Status: Superseded for this specific feature by DEC-018 — the general principle (don't half-wire a convenience feature against a route that doesn't exist) still holds for anything not explicitly requested.
- Consequence: `ArtistNameSearchField` is a plain required text input with no dropdown; `SpotifySearchArtist`-shaped state (`onSelectArtist`, auto-filling the Spotify URL field) was removed from `ArtistSubmitForm` rather than kept as unused plumbing. Contrast with `LoginForm`/`ContactForm`/`ArtistSubmitForm`'s own submission, where the flow _is_ the point of the page — those keep the full flow and simulate the network call instead (DEC applies differently: simulate the core action, drop the optional enhancement).

**DEC-015: When the source has two alternate implementations, port only the one it actively uses**

- Date: 2026-09-21
- Decision: If a source component has a commented-out alternate (e.g. `CampaignBuilder.tsx` importing `SellerContactModal` with `SellerAccountModal` commented out alongside a note that it's "on hold until real advertiser auth/dashboard services exist"), port only the active import. Do not port the disabled alternate "for completeness."
- Reason: The source author already made this call for a documented reason (a missing backend dependency, matching DEC-006's frontend/backend boundary). Porting the disabled path too would mean maintaining dead code that isn't even reachable in the site we're copying from, and would invite it to silently diverge from a "real" decision no one has actually made yet.
- Status: Accepted
- Consequence: `SellerAccountModal` (password-based advertiser account creation) was not ported as part of the `/tele-ads/create` migration. If real advertiser auth ships later, revisit the source's own commented-out swap-back note rather than inventing a new design.

**DEC-016: A shared-class promotion isn't done until every existing consumer is re-checked, not just the new one**

- Date: 2026-09-21
- Decision: When promoting a bare class to `shared.css` (DEC-013), grep _every_ file that already uses that class name — not just the new migrated page that triggered the promotion — for a stale `styles["x"]` (CSS Module) reference that should now be the plain string.
- Reason: Found as a real, previously-unnoticed bug while extracting `flow-modal`/`flow-modal-card`/`modal-x` out of `SiteChrome.module.css` for the campaign builder's modals: `SiteChrome.tsx`'s role-selection modal's "Log in" span was still written as `styles["route-kicker"]`, left over from _before_ `route-kicker` was promoted to shared.css during the tele-ads pass. It had never broken visibly because the old CSS-Module-hashed rule for it was also never cleaned up — two latent bugs masking each other. Neither would show up just by testing the page that prompted the current promotion.
- Status: Accepted
- Consequence: A promotion's "done" checklist includes: move the rule to `shared.css`, update the triggering file(s), _and_ grep the rest of the already-migrated tree for the same class name before considering it finished.

**DEC-017: Interim, frontend-hosted SendGrid email integration for contact/artist-submit/campaign-submit — a bounded, explicit exception to DEC-006/ARCHITECTURE.md §1**

- Date: 2026-09-21
- Decision: While the real backend doesn't exist yet, `/contact`, `/artists/submit`, and `/tele-ads/create` POST to Next.js Route Handlers living in _this_ frontend repo (`src/app/api/integrations/{contact,artist-submit,campaign-submit}/route.ts`), which call SendGrid directly to (a) notify an internal inbox and (b) send the submitter a confirmation email. Ported near-verbatim from music_mandi-website's own already-working `/api/integrations/*` routes, which already used `@sendgrid/mail` exactly this way — not invented fresh.
- Reason: Explicitly requested by the user so the three forms are "usable like before" during the gap before the backend team's project exists. This is a real, scoped exception to DEC-006 and ARCHITECTURE.md §1 ("frontend never owns API implementation or business rules"), not silent scope-creep — no database, no auth, no persistence; each route is a stateless request → SendGrid call → response.
- Status: Accepted (temporary)
- Consequence: All config is server-only env vars (`SENDGRID_API_KEY`, `SENDGRID_FROM_EMAIL`, `CONTACT_TO_EMAIL`, `ARTIST_TO_EMAIL`, `ADS_TO_EMAIL` — see `.env.example`), never `NEXT_PUBLIC_*`. Each route checks its own env vars are set at request time and fails with a clear 500 rather than silently no-opping. `escapeHtml`/SendGrid setup is factored into `src/lib/email.ts` (the one legitimate dedup across 3 otherwise-independent routes — everything else stays route-specific, matching each form's own fields). **When the real backend ships these same endpoints**, delete the three route files and this exception; the frontend components' `fetch()` calls don't need to change if the real backend is reverse-proxied at the same paths, otherwise update each form's fetch URL. Do not extend this pattern to new forms without a fresh, explicit ask — it is not a general "frontend may add backend routes" precedent.

**DEC-018: Restore the Spotify artist-name autocomplete against an interim, frontend-hosted route — supersedes DEC-014 for this one feature**

- Date: 2026-09-21
- Decision: `ArtistNameSearchField`'s Spotify type-ahead (disabled under DEC-014) is restored, backed by `src/app/api/integrations/spotify-search/route.ts` — another interim frontend-hosted route (same shape of exception as DEC-017), ported near-verbatim from music_mandi-website's own working implementation. It uses Spotify's Client Credentials OAuth flow (server-to-server, no end-user login), with the access token cached in a module-level variable for the life of the warm server instance.
- Reason: Explicitly requested by the user ("we have integrated search artist from spotify i want that into new project also"). DEC-014's reasoning (don't half-wire a convenience feature against a nonexistent route) no longer applies now that the route exists and is real — this isn't a reversal of the _principle_, just a consequence of the route now being real.
- Status: Accepted (temporary, same lifecycle as DEC-017)
- Consequence: New server-only env vars `SPOTIFY_CLIENT_ID`/`SPOTIFY_CLIENT_SECRET` (see `.env.example`) — free to create at the Spotify Developer Dashboard, no paid tier needed for Client Credentials search. `ArtistNameSearchField` now takes a required `onSelectArtist` prop again; `ArtistSubmitForm` wires it to auto-fill the Spotify URL field, matching source exactly. A failed/unconfigured search fails silently into an empty dropdown (per the source's own design — see the component's inline comment) rather than surfacing an error, since typing the artist name manually is always a valid fallback. **When the real backend ships this endpoint**, delete `src/app/api/integrations/spotify-search/route.ts` and this exception.

**DEC-019: Migrate video-heavy components from CSS Modules to Tailwind utility classes, one at a time, starting with HeroCarousel**

- Date: 2026-09-21
- Decision: Explicitly requested by the user, starting with `HeroCarousel` (the home page's video hero/carousel): replace its CSS Module (`HeroCarousel.module.css`, ~320 lines with cascade-order duplicate blocks and several `!important` overrides) with Tailwind utility classes directly on JSX elements, using arbitrary-value syntax (`bg-[#070a12]`, `max-[620px]:...`, `[mask-image:...]`) for anything without a clean named utility, and referencing the legacy `--mm-*` design tokens via `var(--mm-brand1)` etc. rather than hardcoding colors. The CSS Module file is deleted once nothing references it.
- Reason: User request, explicitly framed as a performance investigation — "previous website lag them... I think those are issues from videos." Converting hand-authored cascading CSS (with source-order-dependent overrides across `.hero`/`.hero-carousel`/`.hero-slide`/`.hero-slide.video-hero` and 2-3 breakpoints each) to Tailwind is a **pure authoring-syntax change with no performance effect on its own** — Tailwind classes compile to the same CSS. The actual performance win came from two real bugs found and fixed in the same pass (see below), which the Tailwind rewrite surfaced by forcing a full re-read of the component's logic.
- Status: Accepted
- Consequence:
  1. **Real bug fixed**: slide 1's `<video>` (`TamashaMusicHeader.mp4`, ~2.3MB) had a hardcoded `autoPlay` + `preload="auto"` and was never registered in `videoRefs`, so the play/pause `useEffect` could never pause it — it played continuously in the background from page load onward, regardless of which slide was actually visible, unlike slides 0/2 which correctly deferred (`preload="metadata"`) and paused when inactive. This existed in the **source** (`music_mandi-website`) too, not introduced during migration. Fixed by giving slide 1 the same ref + conditional `preload` treatment as slides 0/2.
  2. **Real bug fixed**: `AUTOPLAY_MS = 6500000` (~108 minutes) — almost certainly a typo (extra `000`) for `6500` (6.5s) — meant the carousel never auto-advanced in practice. Also present in source. Fixed to `6500`; flagged explicitly to the user in case the de-facto-static behavior was actually preferred.
  3. Cascade-resolution requires care: several of the legacy `@media` blocks turned out to be **dead code** in the merged `.hero`+`.hero-carousel` cascade (a later, unconditional rule silently overrode them) — confirmed by comparing computed styles before/after at 3 breakpoints (desktop, ~800px, 375px mobile) rather than trusting a visual read of the source CSS alone. One live breakpoint override (`min-height: 720px` at ≤620px) was missed on the first pass and only caught by this same computed-style diffing — **future component conversions in this series must diff computed styles at each breakpoint against a pre-change baseline, not just eyeball a screenshot**, since dead/overridden rules in the legacy CSS are easy to misjudge as live.
  4. `TeleAddCalling.mp4` (another always-autoplaying video, same bug pattern) is used directly in `src/app/(public)/page.tsx`, outside HeroCarousel — flagged for a likely follow-up, not fixed in this pass (out of the requested scope).

**DEC-020: Convert SiteChrome from CSS Modules to real (non-arbitrary) Tailwind classes, split into SplashScreen/AnnouncementBar/Nav/CustomCursor**

- Date: 2026-09-22
- Decision: `SiteChrome.tsx`/`SiteChrome.module.css` (loader, announcement bar, nav, mobile menu, login-role modal, custom cursor all in one file) are replaced by four components — `SplashScreen.tsx`, `AnnouncementBar.tsx`, `Nav.tsx` (nav + mobile menu + login modal), `CustomCursor.tsx` — each mounted independently in `src/app/(public)/layout.tsx`. Unlike DEC-019's HeroCarousel conversion, this one explicitly avoids arbitrary-value syntax (`w-[309px]`, `bg-[#070a12]`, `max-[620px]:...`) per user request ("I want real tailwind classes, not brackets"). Every legacy pixel value, color, breakpoint, easing curve, and compound transition that isn't already a native Tailwind utility is instead registered as a named token or `@utility` class in a new file, `src/components/public/PublicDesignTokens.theme.css` (imported from `globals.css`), using Tailwind v4's own `@theme`/`@utility` extension mechanisms — e.g. `--color-mm-brand-1`, `--spacing-mm-chrome-top`, `--breakpoint-mm-md`, `grid-cols-mm-nav`, `transition-mm-nav`. Values that already land on Tailwind's default scale (e.g. 14px → `py-3.5`) are used directly instead of being re-registered.
- Reason: Explicit user requests, in order: (1) convert SiteChrome's CSS to genuine Tailwind classes, no bracket arbitrary values; (2) split the monolithic component into one simple component per visual phase (splash, banner, nav+menu+modal, cursor), each wired up directly in the layout, "not that typical" do-everything component.
- Status: Accepted
- Consequence:
  1. **Two real bugs found and fixed while building the theme file, both confirmed via computed-style inspection in the browser, not a visual read of the CSS** — the standard this codebase already holds itself to (DEC-019 consequence 3):
     - `@theme` (non-`inline`) mirrors every key to a `:root`-level custom property and resolves that key's own `var(--mm-*)` reference *there*. But `--mm-*` (ink/paper/brand/pad/ease) are defined on `body.mm-public-body`, never at `:root` (see `public-base.css`), so the resolution permanently fails and every descendant inherits the already-broken value — `bg-mm-ink/74` computed to fully transparent, the announcement's 3-stop brand gradient computed to three transparent stops. Fixed by declaring `PublicDesignTokens.theme.css`'s tokens under `@theme inline` instead, which substitutes each value directly into the utility classes that use it (resolved where the class is actually applied, always under `body.mm-public-body`) rather than through the broken `:root` indirection.
     - `--z-*` is **not** an extensible Tailwind v4 theme namespace (unlike `--color-*`/`--spacing-*`/`--text-*`/etc.) — a `--z-mm-*` block inside `@theme` silently generated no utility classes at all (confirmed: `.z-mm-nav` was absent from the compiled stylesheet, computed `z-index` was `auto` everywhere). Fixed by defining z-index as plain `@utility z-mm-nav { z-index: 850; }`-style rules instead.
  2. **A third real, pre-existing bug surfaced and was corrected**: `shared.css`'s ambient `.mm-public a, .mm-public button, .mm-public input { font: inherit; color: inherit; }` is a plain, unlayered rule, so per CSS cascade-layer rules it always wins over a same-property Tailwind utility (which lives in a lower-priority layer) regardless of specificity — it silently resets font-size/weight/line-height back to inherited on every anchor/button that isn't also carrying a higher-specificity unlayered class like `.btn`. This broke the mobile menu's link/login text sizing (`text-mm-mobile-link` computed to the browser default 16px instead of `12vw`, confirmed via `getComputedStyle`), the desktop nav links, the hamburger icon size, and the announcement's close button and "Watch →" link. Fixed by adding Tailwind's `!` important modifier (canonical trailing form, e.g. `text-mm-9!`) to exactly the font-size/weight/line-height utilities on elements where shared.css's ambient rule would otherwise win — same category of issue as the pre-existing `.btn`/`.btn.light` override on the nav's Submit Music button (which already needed the same treatment).
  3. AnnouncementBar and Nav no longer share any JS state for the "nav snaps to top when the announcement is dismissed" behavior — Nav reacts via a plain CSS sibling-selector rule (`.mm-announcement.mm-hide ~ .mm-nav { top: 0; }`) keyed off literal marker classes, requiring AnnouncementBar to render before Nav in `layout.tsx` (documented inline there). A relational selector has no Tailwind equivalent, arbitrary or otherwise, so this stays plain CSS rather than becoming shared state — consistent with keeping each split-out component simple per the second user request above.
  4. **A fourth real, reproducible rendering bug** was found in `SplashScreen`'s entrance effects (the "MUSIC MANDI" letter reveal and the progress-bar fill), both ported as CSS `@keyframes` animations with `animation-fill-mode: forwards` ending at `transform: none`, starting from a Tailwind transform utility (`translate-y-mm-rise rotate-6`, `-translate-x-full`). The Web Animations API and `getComputedStyle` both confirmed these animations reached `playState: "finished"` with a true identity transform — but the browser kept *painting* each element at its pre-animation offset (letters ~30-40px below their box, fully clipped by the row's `overflow-hidden`; the progress fill sitting 150px left of the track, i.e. still at its starting `-100%`). Isolated by toggling one class at a time on a live page (confirmed via `getBoundingClientRect`): removing only the `animate-*` class fixed the position, and rewriting the keyframe with explicit `from`/`to` values (instead of relying on an implicit 0% state) made no difference — the bug is specific to a CSS-animation `fill: forwards` hold following a large-magnitude starting transform, not to keyframe syntax. Fixed by replacing both with a mount-triggered CSS **transition** instead (a `risen` state flipped via `requestAnimationFrame` in a `useEffect`, swapping between the starting-transform classes and `translate-y-0`/`translate-x-0`) — an ordinary style-to-style interpolation, which doesn't hit this bug since there's no fill-mode hold involved. `--animate-mm-rise`/`--animate-mm-load` and their `@keyframes` were removed from `PublicDesignTokens.theme.css` as a result (no remaining consumers).
  5. The desktop nav's dead `nav-login-btn` CSS (styling for a `#loginOpen` button) was not ported — the user's own concurrent edit to `SiteChrome.tsx` (uncommitted at the start of this task) had already deleted the commented-out JSX for that button, confirming it's an intentionally-disabled control, not a bug to fix. Consistently, the mobile menu's `#mobileLogin` button was also commented out (by the user, concurrently, mid-task) and then, on the user's confirmation, removed entirely along with `openRoleModal`/`roleModalOpen` state and the `#roleModal` role-selection modal JSX — with login disabled on both entry points, the modal had no remaining trigger. Login has no UI-only demo entry point anywhere in the nav for now; revisit `/login`/`/tele-ads/login` wiring when real auth is scoped.
  6. Verified in-browser at desktop (1440px), tablet (~930px, 1180px/950px breakpoints), and mobile (375px/390px) widths: splash screen (letters and progress bar both animate in and land fully visible/aligned, confirmed via `getBoundingClientRect`), announcement bar (including dismiss → nav snap-to-top), full desktop nav with the gradient Submit Music button, and hamburger → full-screen mobile menu (links, Submit Music). No console errors. `pnpm lint`, `pnpm build` (typecheck), `pnpm test`, and `pnpm test:e2e` all pass.

**DEC-021: Re-convert HeroCarousel to eliminate arbitrary-value syntax; rename the shared theme file from `PublicChrome.theme.css` to `PublicDesignTokens.theme.css`**

- Date: 2026-09-22
- Decision: `HeroCarousel.tsx` (converted to Tailwind under DEC-019, which explicitly allowed arbitrary-value syntax) is re-converted to use only real Tailwind classes — no `bg-[...]`, `max-[620px]:...`, `text-[...]`, or `bg-(--css-var)` shorthand — following the same standard set for SiteChrome's split in DEC-020. New one-off values (the decorative background grid, the video-readability shade gradients, the hero heading's clamp/tracking/leading/text-shadow, the `4%` slide-shift translate, the copy column's `min()`-based max-widths, the prev/next button color, the inactive-dot color/the active-dot pill radius) are registered as named `@theme`/`@utility` entries. Values already on Tailwind's default dynamic scale (`pt-32.5`, `h-9.5`, `gap-1.75`, etc. — confirmed via the compiled stylesheet to generate correct `calc(var(--spacing) * N)` rules for arbitrary decimals, not just quarter-step presets) are left as-is. Breakpoints/colors HeroCarousel shares with the chrome components (`--breakpoint-mm-sm`/`-md`, `--color-mm-ink`, `--color-mm-brand-1`/`-2`) are reused rather than re-registered. The file housing all these tokens, `src/components/public/PublicChrome.theme.css`, is renamed to `src/components/public/PublicDesignTokens.theme.css` (with a matching `globals.css` import update) since it now serves more than the chrome components — a second component (HeroCarousel) needing the same breakpoint/color tokens makes "chrome" an inaccurate name going forward, matching this codebase's own DEC-013 precedent of promoting/relocating shared values reactively rather than speculatively.
- Reason: Explicit user request to apply the same "real Tailwind classes, not brackets" standard to HeroCarousel next, extending the direction set for SiteChrome in DEC-020.
- Status: Accepted
- Consequence:
  1. HeroCarousel's logic (autoplay timer, video play/pause on active-slide change, prev/next/dot navigation) is untouched; this is a class-authoring pass. Verified pixel-equivalent to the pre-change render at desktop (1440px) and mobile (390px) for all 3 slides (video shade, gradient heading text, dot/arrow controls).
  2. **A real, user-reported bug was fixed while doing this pass**: slide 0's mobile heading used `50vw` (`--text-mm-hero-mobile-lg`) versus slides 1/2's `15vw` (`--text-mm-hero-mobile`) for the identical role — over 3x larger, and confirmed broken (text overflowing past both screen edges, letters cut off) rather than an intentional design choice. Initially ported faithfully as-is (matching this codebase's usual "don't fix what wasn't asked" discipline), but the user directly reported it visually breaking on mobile, which settles it as a genuine pre-existing bug rather than a deliberate characteristic — same category as DEC-019's `AUTOPLAY_MS` typo. Fixed by pointing slide 0 at the same `--text-mm-hero-mobile` (`15vw`) token slides 1/2 already use; `--text-mm-hero-mobile-lg` removed from the theme file (no remaining consumers). Verified in-browser: slide 0's mobile heading now renders at the same well-fitting size as the other two slides.
  3. One coincidental value collision was raised by Tailwind's IDE tooling but not resolved the way suggested: it flagged `mt-6.5`/`h-9.5` as equal to already-registered chrome tokens (`mt-mm-lg` = 26px, `h-mm-chrome-top-sm` = 38px) and offered to rewrite them. The reasoning to decline stands — reusing a token whose *name* describes an unrelated concept (a loader logo's margin, an announcement bar's mobile height) for a hero CTA's margin or a carousel button's size reads as a copy-paste mistake to a future maintainer, even though the numbers match — but the editor's format-on-save applied the rewrite anyway for `mt-6.5` → `mt-mm-lg` before this could be enforced; left as the editor set it, since it's CSS-identical either way and not worth fighting the tooling over on every save.
  4. `pnpm lint`, `pnpm build` (typecheck), `pnpm test`, and `pnpm test:e2e` all pass.

**DEC-022: Fix the Tamasha banner image leaving a black gap in its frame on narrow/short aspect ratios**

- Date: 2026-09-22
- Decision: In `src/app/(public)/page.module.css`, `.tm-banner-photo-frame .tm-banner-image` (the effective, latest-cascade rule for the `<Image>` in the home page's `#tamasha-launch` section) is changed from a normal-flow `display:block; width:100%; height:auto` element to `position:absolute; inset:0; width:100%; height:100%; aspect-ratio:auto`, filling its frame completely instead of sizing itself independently.
- Reason: User-reported bug — the image showed a solid black empty area beneath it on mobile. Root cause: `.tm-banner-frame` carries its own `min-height` (`min(72vh, 760px)` desktop, a flat `360px` at ≤620px — two separate, cascade-order-resolved rules, matching this codebase's recurring "chronological CSS patches" pattern per DEC-010) completely independent of the image. The image itself kept `height:auto` with an `aspect-ratio:16/9` from an earlier-in-file `.tm-banner-image` rule that was never overridden, so its rendered height is purely `width × 9/16` — on a 390px-wide phone that's ~190px, versus the frame's forced 360px minimum, leaving ~170px of the frame's own dark background (`#0b0b0b`) exposed below the image. The frame was already `position:relative; overflow:hidden` — a cropping-viewport pattern with no viewport-filling child — so the fix makes the image (which already had `object-fit:cover` from that same earlier rule, also never overridden) absolutely fill it instead.
- Status: Accepted
- Consequence: Verified in-browser at mobile (390px, where the gap was worst) and desktop (1440px) — the image now fills the frame edge-to-edge at both the mobile fixed 360px and desktop `min(72vh,760px)` heights, cropped via `object-fit:cover`, no visible background gap. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass. Scoped narrowly to this one image/frame pair, per the user's ask — no broader pass over `page.module.css`'s other cascade-duplicate blocks (e.g. `.tm-launch h2`/`.product-head p` etc. have the same two-pass-CSS pattern) was requested or made.

**DEC-023: SplashScreen — pack the logo and wordmark as one centered block instead of letting their grid rows stretch apart**

- Date: 2026-09-22
- Decision: `.loader-mark` (the `grid place-items-center` wrapper around the logo `<Image>` and the "MUSIC MANDI" wordmark row in `SplashScreen.tsx`) gains `content-center` (`align-content: center`) alongside its existing `place-items-center`, and the logo's old `mb-mm-lg` (26px) bottom margin is replaced with a `gap-4` on the wrapper.
- Reason: User request — wanted the wordmark to sit close beneath the logo with a small, standard gap. The margin swap alone (`mb-mm-lg` → `gap-4`) measured as a 367px gap in-browser, not 16px: `.loader-mark` sits in the loader's own `1fr` grid row (`grid-rows-mm-loader`) and stretches to nearly the full loader height; with only `place-items-center` and no `align-content` set, its two implicit `auto` grid rows (logo, wordmark) each individually stretch to fill about half of that height (`align-content: normal` resolves to `stretch`-like behavior for `auto`-sized tracks when there's leftover space in a grid) — centering the logo and the text far apart in their own oversized tracks rather than packing them together. `content-center` packs both rows at their natural content size first, then centers that compact pair as a single unit within the tall space.
- Status: Accepted
- Consequence: Verified via `getBoundingClientRect` that the gap between the logo's bottom edge and the wordmark's top edge is exactly 16px (matching `gap-4`), and confirmed visually at mobile (390px) and desktop (1440px) — logo and wordmark now read as one grouped, centered block. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass.

**DEC-024: Convert PartnerRunway to Tailwind favoring simplicity over exact per-logo cascade fidelity**

- Date: 2026-09-22
- Decision: `PartnerRunway.tsx`/`PartnerRunway.module.css` (a ~200-line CSS Module with the same "chronological patches" structure as other pre-Tailwind files — several dead/superseded blocks, e.g. `.partner-logo-card`'s own 260×100px sizing and 18px border-radius, both later overridden to 250×92px and 0 by a later `!important` block) is converted straight to Tailwind classes on JSX, skipping the meticulous per-value theme-token registration used for SiteChrome/HeroCarousel (DEC-020/021) — explicit user request this time was for the simplest working Tailwind, not exhaustive fidelity to every legacy attribute-selector override. Only one genuinely custom piece was needed: the infinite marquee (`@keyframes partner-runway-scroll` + `@utility animate-marquee`, added to `PublicDesignTokens.theme.css`), since no native Tailwind utility does an infinite horizontal loop. Everything else — sizing, colors, spacing — maps to plain Tailwind classes (`min-h-57.5`, `gap-5`, `h-23 w-62.5`, etc., using the confirmed-working dynamic decimal spacing scale) or reuses tokens already shared from prior components (`bg-mm-navy`, `left-mm-gutter`, `max-mm-sm:`).
- Reason: Explicit user request ("use simple tailwind... as simple as possible... write simplest approach"), a different priority than the pixel-exact standard set for SiteChrome/HeroCarousel.
- Status: Accepted
- Consequence:
  1. The source CSS had per-logo image-sizing overrides keyed off `[alt*="Sony"]`/`[alt*="Orchard"]`/`[data-tm-logo]` attribute selectors — a pattern Tailwind has no simple utility equivalent for. Resolving the actual cascade showed every one of those per-logo `max-width`/`max-height` caps was **non-binding** in practice (each logo's own aspect ratio already keeps it under its cap at the shared 58px/46px height) except one real 2px difference: Tamasha's mobile height computed to 44px vs. 46px for the other three. Given the explicit "simplest approach" instruction, this 2px, visually-imperceptible discrepancy was not preserved — all four logos now share one uniform size (`h-14.5` desktop, `max-mm-sm:h-11.5` mobile). The now-unused `data-tm-logo` JSX attribute (which existed solely to drive that CSS attribute selector) was removed along with it.
  2. The card's explicit padding (`12px 28px` desktop / `10px 20px` mobile in the source) was dropped entirely — `place-items-center` on a fixed-size card already centers the (smaller) logo image without it, so the padding was redundant given the current image sizing, not a visible design element.
  3. Added a `Logo` TypeScript type for the `LOGOS` array (previously untyped inline objects), per the user's mention of wanting TypeScript addressed too.
  4. Verified in-browser at desktop (1440px) and mobile (390px): all four logos render at consistent, correctly-proportioned sizes, the marquee scrolls smoothly and loops seamlessly, matching the pre-change visual. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass. `PartnerRunway.module.css` deleted (no remaining consumers).

**DEC-025: Adopt `docs/TYPESCRIPT_TAILWIND_REWRITE_REFERENCE.md` as the standard for future component conversions; merge OnePlatformSection + DashboardMock**

- Date: 2026-09-22
- Decision: The user added `docs/TYPESCRIPT_TAILWIND_REWRITE_REFERENCE.md` to the repo and asked for it to govern all future TS/Tailwind conversions — its explicit priority order ranks "standard Tailwind utilities" and "simple code" above "exact pixel-level matching," the opposite emphasis from DEC-020/021's exhaustive per-value theme-token registration for SiteChrome/HeroCarousel. `OnePlatformSection.tsx` + `DashboardMock.tsx` (previously two files/components, ~340 lines of CSS Modules combined) are merged into one file — `DashboardMock` is now a non-exported function local to `OnePlatformSection.tsx` — since `DashboardMock` is only ever rendered inside `OnePlatformSection`, nowhere else, and the combined result reads as one clear section rather than two arbitrarily split files.
- Reason: Explicit user request, both to formalize the new conversion standard and to merge these two specifically ("they are same component part").
- Status: Accepted
- Consequence:
  1. The merge makes the combined file a client component (`"use client"`) throughout, since `DashboardMock`'s pointer-tilt effect needs it — a minor step back from FRONTEND_STANDARDS.md's "default to Server Components" preference, accepted here since `OnePlatformSection`'s only other content (`Reveal`-wrapped text) was already effectively client-rendered anyway (`Reveal` itself is a client component), so the outer shell being server-only bought little in practice.
  2. Per the new reference doc, layout ratios/breakpoints were simplified to Tailwind's standard scale rather than replicated exactly: the legacy `950px` stacking breakpoint became `sm:` (640px); the headline/paragraph grid's asymmetric `1.05fr/0.8fr` columns became plain equal `sm:grid-cols-2`; the dashboard card's `min(1100px,92vw) × min(610px,58vh)` sizing became `max-w-5xl` with **no forced aspect ratio** (letting content size it, since the original ~1.8:1 ratio wasn't a clean standard Tailwind `aspect-*` value either); 9px/10px text became `text-xs` (12px, Tailwind's smallest named size); the release table's `1.4fr/0.8fr/0.8fr/0.8fr` columns became a `grid-cols-5` grid with the first column spanning 2 (a standard-utility way to keep one column wider without arbitrary fr values — needed after the naive equal-`grid-cols-4` first pass visibly overflowed the "Latest releases" header into the next column, caught by screenshot verification). Colors not already shared as `mm-*` tokens (the dashboard card background, metric-card tiles, borders, the second floating tag's accent, the chart line) were mapped to Tailwind's own default palette (`slate-900`, `white/5`, `white/10`, `violet-600`/`pink-600`, `blue-500`) instead of registering more custom hex tokens.
  3. `id="dashboard"` (needed by `CustomCursor.tsx`'s hover-grow effect, which matches literal `a,button,.cursor-hover-target,#dashboard` — see DEC-012) was nearly dropped during the rewrite since it wasn't visually load-bearing; re-added and verified the cursor-grow interaction still fires over the mockup card. A second real bug from the initial draft was caught before shipping: mapping the release rows with a shorthand `<>...</>` fragment doesn't accept a `key` prop, so React would have received unkeyed fragments in a list — fixed by using `<Fragment key={...}>` from `react` instead.
  4. Verified in-browser at desktop (1440px) and mobile (390px, sidebar/chips/release-table intentionally hidden below `sm:`, matching the source's own mobile simplification), and confirmed the pointer-tilt interaction still fires (dispatched a synthetic `pointermove`, observed the card's `transform` change from its resting `rotateX(7deg) rotateY(-8deg)`). `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass. `DashboardMock.tsx`, `DashboardMock.module.css`, and `OnePlatformSection.module.css` deleted.

**DEC-026: Convert SystemsCarousel to the new rewrite standard, keeping its pinned-scroll logic untouched**

- Date: 2026-09-22
- Decision: `SystemsCarousel.tsx`/`.module.css` (a horizontally-scrolling "pinned section" carousel — a 360vh-tall section holds a `position: sticky` viewport that stays pinned to the top of the screen while a scroll listener translates the card track horizontally based on scroll progress) is converted to Tailwind under the same standard as DEC-024/025. The scroll-progress `useEffect` (reading `section.offsetHeight`/`window.scrollY`, rAF-batching, setting `activeIndex`) is untouched — it's plain JS logic, not CSS, and already correct. Two values are kept as justified arbitrary Tailwind values rather than forced onto the standard scale: `h-[360vh]` on the section (the scroll-progress effect reads this element's own height to compute how much scrolling maps to the full card sweep — an exact, functionally load-bearing value, not a decorative one) and `h-[calc(100svh-76px)]` on the sticky inner wrapper (paired with `top-19`/76px, so the sticky element's bottom edge exactly meets the viewport's bottom edge — the two values are a real functional pair, not independent choices). Both meet the reference doc's own stated bar for a rare, allowed arbitrary value.
- Reason: Same standard as DEC-025, applied to the next component in this migration pass.
- Status: Accepted
- Consequence:
  1. The CSS's fluid `clamp()`-based type scale and card width (`clamp(300px,31vw,470px)`) became stepped standard Tailwind sizes (`text-3xl sm:text-4xl lg:text-5xl`, `w-75 sm:w-100 lg:w-115`) instead of continuous fluid scaling — per the reference doc's explicit preference for standard utilities over replicating every fluid curve.
  2. The `::after`-based hover-reveal arrow (`content: "↗"`, translate+opacity transition) was rebuilt as a real `<span>` with Tailwind's `group`/`group-hover:` pattern instead of `after:content-['']` — consistent with this session's established preference (see the Nav.tsx underline from earlier work) for a real DOM node over the one Tailwind pattern that still requires bracket syntax.
  3. Two nearly-dead pieces of the source CSS were dropped as genuinely inert rather than ported: `scrollbar-width: none`/`::-webkit-scrollbar{display:none}` on the track (the track is horizontally positioned via a JS `transform`, never natively `overflow-x: auto`, so no scrollbar can ever appear there to hide) and the literal `id="systemsTrack"` attribute (confirmed via repo-wide search to be read by no code — the React component uses a `ref`, not `getElementById`). The `.section-title`'s `flex/justify-between/gap` setup (clearly shared with other "section-title" usages elsewhere on the site that pair a heading with a second flex item) was also dropped, since this component's own usage has only one child — the property was inert here specifically, confirmed by inspecting the actual JSX before assuming it mattered.
  4. The literal `cursor-hover-target` marker class (read by `CustomCursor.tsx`'s hover-grow effect via `closest()`, per DEC-012) was kept as-is, since it's a cross-component DOM contract, not a styling concern Tailwind can express.
  5. Verified in-browser at desktop (1440px) and mobile (390px): all 9 cards render with correct sizing/spacing, the active (scroll-driven) card and the hover state both show the correct brand-gradient background, the corner arrow reveals only on real mouse hover (not on scroll-driven "active", matching the source's own distinction), and scrolling the page confirms the pinned-sticky mechanic and horizontal card advance both still work exactly as before. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass. `SystemsCarousel.module.css` deleted.

**DEC-027: Convert DspMarquee to the new rewrite standard**

- Date: 2026-09-22
- Decision: `DspMarquee.tsx`/`.module.css` (a two-row, opposite-direction infinite logo marquee below a "100+ platforms" stat block) is converted under the same standard as DEC-024–026. Resolving the cascade first (this file had 3+ chronological rewrite passes) showed the entire original `.stats` block — a 4-column, viewport-height-based layout with its own background gradient — was completely dead, superseded end-to-end by the later `.dsp-section`/`.dsp-real` rules (fixed `520px` min-height, `0.7fr/1.3fr` grid, `#080c17` background); none of it was ported. The asymmetric `0.7fr/1.3fr` split became a standard `grid-cols-12` with `col-span-4`/`col-span-8` (≈33/67%, close to the original ≈35/65%), consistent with the same technique used for OnePlatformSection's grid in DEC-025.
- Reason: Same standard as DEC-025/026, applied to the next component in this migration pass.
- Status: Accepted
- Consequence:
  1. Two small custom marquee utilities were added to `PublicDesignTokens.theme.css` (`animate-dsp-scroll`/`animate-dsp-scroll-reverse`, sharing one `dsp-marquee-scroll` keyframe at two different durations, one reversed) — the same category of justified custom addition as `animate-marquee` (DEC-024), since no native Tailwind utility does an infinite two-speed marquee pair.
  2. Hover interactions needed **nested named groups** (`group/belt` on the row wrapper, `group/card` on each logo tile) since two independent hover behaviors compose here: hovering anywhere on the belt pauses that row's scroll (`group-hover/belt:paused` — confirmed `paused` is a real named Tailwind utility for `animation-play-state`, not requiring a bracket value), while hovering one specific card reveals its logo in full color (`pointer-fine:group-hover/card:...`, chaining the standard `pointer-fine:` variant so the effect doesn't stick on touch devices — matching the source's own `@media(hover:hover) and (pointer:fine)` guard). One `mask-image` gradient (fading the row's left/right edges) was kept as a justified arbitrary value — no standard Tailwind utility expresses an edge fade, and the effect is visually real, not decorative filler.
  3. A real layout bug was caught and fixed during verification: the "100+ global platforms" badge is `position: absolute` (pinned top-right) on desktop and `static` (stacked inline) only on narrow screens in the source — the first draft here had the responsive direction backwards (`sm:static` instead of the reverse), stretching the badge into a full-width bar at desktop widths. Caught via screenshot comparison against the pre-change render, not assumed correct from reading the class list.
  4. Verified in-browser at desktop (1440px) and mobile (390px): layout, big "100+" numerals, and the badge's corrected position all match the source; confirmed via real `page.mouse.move` (not JS-dispatched events, which don't trigger CSS `:hover`) that hovering a belt pauses only that row's animation, and a screenshot taken mid-hover shows the targeted logo rendering in full color/enlarged while its neighbors stay grayscale. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass. `DspMarquee.module.css` deleted.

**DEC-028: Convert FairRightsSection to the new rewrite standard, fixing the pre-existing `.orbit` 0×0 bug**

- Date: 2026-09-22
- Decision: `FairRightsSection.tsx`/`.module.css` is converted under the same standard as DEC-024–027. Unlike the previous four components, this one includes a genuine **behavior fix**, not just a styling rewrite: the source's `.orbit` container (holding the rotating ring + gradient "Your rights stay yours." core) had no explicit width — only `aspect-ratio: 1` plus a `max-width` — and its only children are `position: absolute`, which contribute nothing to a parent's auto-sizing. The container computed to 0×0, so the ring was invisible and the core shrank to fit its own text instead of forming a circle — confirmed both via a fresh screenshot and matching an already-documented note elsewhere in this repo's history about this exact issue. `w-full` on the container gives `aspect-ratio` a real width to resolve from; the ring and core were initially given explicit fixed diameters (`h-130 w-130`, `h-65 w-65`) centered with `inset-0 m-auto` — **superseded the same day by DEC-029**, which replaced the fixed diameters with percentage sizing once mobile overflow surfaced.
- Reason: The reference doc's stated priority order puts "correct behavior" first and explicitly permits fixing "obvious inconsistencies" while converting (rule 18); a 0×0 decorative element with invisible content is not a subjective design choice, it's a rendering bug, and this pass touches exactly the code where it lives.
- Status: Accepted
- Consequence:
  1. The ring's two dots (`::before`/`::after` in the source) became real `<span>` elements — consistent with this session's established preference for a real DOM node over `content-['']`.
  2. A custom `animate-orbit-spin` utility/keyframe (22s, matching the source) was added to `PublicDesignTokens.theme.css`, named distinctly from `PartnerRunway`'s/`DspMarquee`'s marquee keyframes and from the *other* `@keyframes spin` this same codebase already has scoped inside `FinalCtaSection.module.css` (per this repo's own history: `spin` was intentionally duplicated per-file when the old `page.module.css` was split, since each CSS Module scopes the name independently) — the new shared global file has no such per-file scoping, so a distinct name was required to avoid a real collision once `FinalCtaSection` is converted next.
  3. Verified in-browser at desktop (1440px) and mobile (390px): the ring and core now render as full circles exactly as the design clearly intended, confirmed the spin animation is running at the correct 22s duration via the Web Animations API, and confirmed layout/copy/rights-list match the source. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass. `FairRightsSection.module.css` deleted.

**DEC-029: Replace FairRightsSection's fixed-diameter orbit ring/core with percentage-of-parent sizing**

- Date: 2026-09-22
- Decision: DEC-028's fixed diameters (`h-130 w-130` = 520px ring, `h-65 w-65` = 260px core) don't shrink with their container, so on mobile the ring overflowed its column significantly — confirmed via `getBoundingClientRect()`: the orbit container measured only ~333px wide at a 390px viewport, well under the 520px ring. Since the container now has a definite size (`w-full` + `aspect-square`, from DEC-028's own fix), absolutely-positioned children can safely use percentage width/height instead of a fixed pixel size, restoring the source design's original percentage-relative approach (its dead `.orbit` rule used `inset: 8%`/`inset: 29%`, which never worked because the container itself was 0×0). Two new utilities were added to `PublicDesignTokens.theme.css` — `size-mm-orbit-ring` (84%×84%) and `size-mm-orbit-core` (42%×42%) — since Tailwind's size utilities are either the fixed spacing scale or a small enumerated set of fractions (halves, thirds, quarters, etc.) with nothing at 84%/42%, and an arbitrary-value class (`w-[84%]`) would reintroduce the bracket syntax the rewrite standard avoids. The two percentages were chosen to reproduce DEC-028's fixed sizes almost exactly at the container's `max-w-155` (620px) ceiling — 84% × 620px ≈ 521px, 42% × 620px ≈ 260px — so desktop is visually unchanged.
- Reason: User report — "make this responsive in mobile size screen its overflow that should smaller according to screen size." Percentage-of-parent sizing is correct at every viewport width by construction, not just at the two breakpoints that happened to get checked, which better satisfies the reference doc's "correct behavior" priority than a breakpoint-stepped set of fixed sizes would.
- Status: Accepted
- Consequence:
  1. `h-130 w-130` on the ring and `h-65 w-65` on the core were replaced with `size-mm-orbit-ring`/`size-mm-orbit-core`; the `inset-0 m-auto` centering was kept unchanged (it centers a percentage-sized box the same way it centers a fixed one).
  2. Verified via `getComputedStyle` and screenshots at 390px, 820px, and 1440px: the ring/core width always resolves to exactly 84%/42% of the live container width, never exceeds it, and desktop sizing is pixel-equivalent to DEC-028's fixed values. (An initial `getBoundingClientRect()` check appeared to show the rotating ring still overflowing at ~372px against a 327px container — this was a measurement artifact of reading a rotated element's axis-aligned bounding box mid-animation, not a real overflow; `getComputedStyle().width` confirmed the true circle diameter was 274.8px, correctly under the container width.)
  3. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass.

**DEC-030: Replace the orbit core's live text with the `orbit-badge.svg` design asset**

- Date: 2026-09-22
- Decision: DEC-029 made the ring/core scale correctly, but the core's live text (`Your rights` / `stay yours.` at `clamp(22px, 3vw, 44px)`, matching the original CSS exactly) still visually overflowed its own circle at real mobile widths (confirmed via screenshot at 375px and 320px) — the core shrinks faster than a fixed-ratio font clamp can track once padding is taken into account, a limitation of the original design itself, not just this port. The user supplied the actual design asset for this badge — `your_rights_stay_yours.svg`, a 295×253 graphic combining the two accent circles and the "YOUR RIGHTS STAY YOURS." wordmark as vector paths — and asked to use it directly instead of continuing to fight text reflow. It's saved as `public/images/orbit-badge.svg` and rendered via `next/image` with `fill` + `object-contain` inside the existing `size-mm-orbit-core` percentage box, so the whole badge (circles + wordmark) now scales as one indivisible unit and can never wrap or overflow independently of its container.
- Reason: Explicit user instruction, after two rounds of text-sizing fixes (breakpoint steps, then the original's own `clamp()`) both still overflowed at realistic phone widths — a vector asset sidesteps text-reflow entirely, which is a more correct fix than continuing to chase font-size/padding tuning for a shrinking circular text container.
- Status: Accepted
- Consequence:
  1. The core `<div>`'s bold/uppercase live text, its gradient background, and the now-dead `text-mm-orbit`/`leading-mm-orbit` theme tokens were removed; `size-mm-orbit-core` (still 42%×42%) now sizes an `Image` instead of a text container.
  2. The rotating ring and its two pink accent dots (`.orbit-ring`'s CSS-confirmed final styles) are untouched — only the inner core changed.
  3. Verified via screenshot at 375px and 1440px: the badge renders fully inside the ring at both sizes with no reflow risk at any width in between, since it scales as a single image rather than reflowing text. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass.

**DEC-031: Convert TamashaLaunchSection to the new rewrite standard**

- Date: 2026-09-22
- Decision: `TamashaLaunchSection.tsx`/`.module.css` converted under the same standard as DEC-024–028. This file carries its own "chronological patches" history even though it was already extracted as a standalone component: the section element has BOTH `styles.tamasha` and `styles["tm-launch"]` classes applied together, and the CSS module has two nearly-independent rule sets for them (an older large dark "hero" treatment under `.tamasha`, followed by a newer compact yellow "launch strip" treatment under `.tm-launch`, the latter using `!important` almost everywhere). Resolving the actual final cascade (same method as DEC-024–030) shows `.tamasha`'s contribution is now 100% dead — every property it sets (`min-height`, `background`, `color`, padding, and all three `h2` properties) is overridden by an unconditional or `!important` `.tm-launch` rule later in the file — so only `.tm-launch`'s final effective values were ported. Notable resolved values: the section background is `#ffcf38` (a yellow, not the dark gradient the earlier `.tamasha` block suggests) with `#0b0b0b` text (→ `bg-amber-300`/`text-neutral-950`, closest standard Tailwind swatch under the "standard over custom" doctrine); `.tm-launch-grid` never actually sets `display: grid` (its `grid-template-columns` media rule is dead), so the layout is a plain block stack, not a grid — the wrapper `<div>` for it and for `.tm-logo-wrap` (which only ever styled its child `<img>`, never itself) were both dropped as functionless wrappers per the "no unnecessary abstraction" rule. The banner image swapped `width`/`height` props for Next's `fill` prop, since the final effective CSS already forced it to `position:absolute; inset:0; width:100%; height:100%; object-fit:cover` inside a `position:relative` frame — exactly what `fill` does natively.
- Reason: Same as DEC-024–028 — this is a pure conversion pass per `docs/TYPESCRIPT_TAILWIND_REWRITE_REFERENCE.md`, with the dead-`.tamasha`/dead-grid findings treated as "obvious inconsistencies" safe to drop per that doc's rule 18, not new product decisions.
- Status: Accepted
- Consequence:
  1. Fluid `clamp()`/`vw` sizing for the heading was replaced with standard breakpoint-stepped Tailwind text sizes (`text-5xl sm:text-6xl md:text-7xl`), consistent with the "simple over exact" priority; `tracking-tighter` (-0.05em) and `leading-none` (1) stand in for the original's -0.055em/0.94–0.98 values (negligible visual difference). `break-normal` replicates the load-bearing `overflow-wrap: normal` override (confirmed necessary — `public-base.css` sets `overflow-wrap: break-word` ambiently on `.mm-public`), and `text-balance` replicates `text-wrap: balance`.
  2. The banner frame's border, corner radius, and shadow used exact-percentage/scale matches where available (`border-black/16`, `rounded-3xl` = 24px exactly) and one justified arbitrary value, `min-h-[min(72vh,760px)]` at `md:`, kept because it's a functionally load-bearing viewport-height cap on the crop box, not a cosmetic choice (same reasoning as SystemsCarousel's `h-[360vh]`).
  3. `id="tamasha-launch"` was preserved exactly — confirmed via grep that `AnnouncementBar.tsx` and `Footer.tsx` both link to `/#tamasha-launch`, making it a real cross-component anchor contract, not decorative.
  4. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass; verified in-browser at 1440px and 390px, including confirming the banner frame's height resolves to exactly 648px (72% of a 900px-tall viewport) at desktop. `TamashaLaunchSection.module.css` deleted.

**DEC-032: Convert TeleAdsSection to the new rewrite standard, fixing a real double-margin button-spacing bug**

- Date: 2026-09-22
- Decision: `TeleAdsSection.tsx`/`.module.css` converted under the same standard as DEC-024–031. Same "chronological patches" shape as DEC-031: the section has both `.tone` (an older dark hero treatment) and `.tele-home` (a newer compact treatment) classes together, and the card has `.tone-card` plus a higher-specificity `.tele-home .tone-card` override. Resolving the final cascade: the section is `#0b101b` background / white text (`.tele-home`'s unconditional rules win over `.tone`'s); the card is an unconditionally-560px-tall (`min-h-140`) dark card with a pink-tinted border (`rgba(255,25,114,.22)` — confirmed to be `mm-brand-1` at 22% opacity, so `border-mm-brand-1/22` needed no new token) via `.tele-home .tone-card`'s higher specificity beating every bare `.tone-card` rule regardless of source order; the video wrapper's `.tele-brand-scene{min-height:420px}` is itself dead (a later same-specificity `.tele-video-box{min-height:0}` rule overrides it back to 0, confirmed by checking every rule touching that property in source order). `#0b101b`, `#090d16`, and `#ff5d90` all recur across `shared.css` and the standalone `/tele-ads` route's own files (confirmed via grep, not just this component), so — unlike most one-off colors ported to the nearest Tailwind swatch this pass — these were registered as real named tokens (`mm-tele-ink`, `mm-tele-black`, `mm-tele-pink`) since they're a genuine reused sub-palette, consistent with how `mm-ink`/`mm-navy`/`mm-brand-*` were registered.
- Reason: Same as DEC-024–031 for the conversion itself. Separately, while measuring the button row (`.gap-12`, a flex row with its own `gap:12px; margin-top:26px`) against the older per-button rule `.tone :global(.btn){margin-top:25px}`, live-rendering confirmed (via `getComputedStyle` and a screenshot) a real visual bug: because flex item margins don't collapse, each button independently added another 25px on top of the container's own spacing, producing a ~25px dead gap above the first button and a bloated ~37px gap between the two stacked buttons on mobile (screenshot showed it plainly) instead of the intended even 12px. This is the same "leftover rule predates a later flex-wrapper refactor" pattern already documented in DEC-028/031, safe to drop per the reference doc's rule 18.
- Status: Accepted
- Consequence:
  1. The per-button `margin-top:25px` was NOT ported — the button row now relies solely on the flex container's `gap-3` (12px) and `mt-6.5` (26px), producing even spacing. Confirmed via mobile screenshot: the two buttons now sit with equal, intentional-looking spacing instead of the original's visible gap bug.
  2. A new `grid-cols-mm-tele` utility (`1.1fr 0.9fr`) was added to `PublicDesignTokens.theme.css` for the card's asymmetric column split, and a new `aspect-mm-tele-video` theme value (`1288 / 1608`) for the call-mockup video's real intrinsic ratio — both genuinely have no Tailwind-native equivalent (asymmetric grid fractions and non-standard aspect ratios), same reasoning as `grid-cols-mm-nav` and `bg-mm-grid` earlier in this file.
  3. The subtle two-stop dark gradient on the card (`linear-gradient(145deg,#14101d,#0b101a)`) was simplified to a solid `bg-neutral-950`, since the two stops are close enough in value that the visual difference is negligible — consistent with "simple over exact" once a value isn't a genuine reused constant (unlike the three `mm-tele-*` colors above).
  4. The banner `<video>` switched from `next/image`-style fixed sizing to Tailwind's `aspect-mm-tele-video` + `object-contain`, matching the original's real aspect-ratio-preserving letterbox behavior exactly.
  5. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass; verified in-browser at 1440px and 390px. `TeleAdsSection.module.css` deleted.

**DEC-033: Convert FaqSection to the new rewrite standard, preserving the `.faq-redesign` cross-component contract**

- Date: 2026-09-22
- Decision: `FaqSection.tsx`/`.module.css` converted under the same standard as DEC-024–032. Same dual-treatment "chronological patches" shape as DEC-031/032 (`.faq` older dark full-height treatment, `.faq-redesign` newer compact two-column treatment layered on with `!important`), resolved the same way — only `.faq-redesign`'s final effective values were ported (`bg-mm-ink`/`text-mm-paper`, a `.65fr 1.35fr` asymmetric grid collapsing to one column at `md:`, breakpoint-stepped text in place of the `clamp()`/`vw` heading and paragraph). One thing this component does that none of the previous ones did: the section's className includes a **third, plain, non-CSS-module string** — `"faq-redesign"` — alongside the two CSS-module classes. Grepping confirmed why: `FaqAccordion.module.css` (a separate, NOT-in-scope component) has four `:global(.faq-redesign) .faq-item` / `.faq-q` / etc. rules that give the accordion itself a completely different look (bordered, boxed pill items) whenever an ANCESTOR carries this literal class — the same "unscoped marker class read by another component's own CSS" pattern as `.mm-announcement.mm-hide ~ .mm-nav` (DEC-012) and `id="dashboard"`. This class was kept verbatim on the converted section; `FaqAccordion.tsx`/`.module.css` were not touched at all, matching the task's actual scope.
- Reason: Same as DEC-024–032 for the conversion pass itself. The `.faq-redesign` marker class is a real cross-component contract (confirmed via grep, not assumed), not decorative, so dropping or renaming it — which would have been easy to do by accident once its own CSS module went away — would have silently broken the accordion's intended boxed styling.
- Status: Accepted
- Consequence:
  1. A new `grid-cols-mm-faq` utility (`0.65fr 1.35fr`) was added to `PublicDesignTokens.theme.css`, same reasoning as `grid-cols-mm-tele`/`grid-cols-mm-nav`.
  2. `#070b14` (the resolved final section background) was close enough to the already-registered `mm-ink` (`#070a12`) to just reuse `bg-mm-ink` rather than registering a near-duplicate token — a tighter case of the same "standard over one-off" judgment call already made repeatedly this pass, but here the two values are close enough that no meaningful token would be lost by treating them as identical.
  3. Verified in-browser at 1440px and 390px that the accordion still renders its bordered/boxed "redesign" styling correctly (first item open by default via the untouched `firstOpen` prop), confirming the `:global(.faq-redesign)` contract survived the rewrite. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass. `FaqSection.module.css` deleted.

**DEC-034: Convert FinalCtaSection to the new rewrite standard**

- Date: 2026-09-22
- Decision: `FinalCtaSection.tsx`/`.module.css` converted under the same standard as DEC-024–033 — the last component in this dual-treatment family (`.final` older full-viewport treatment, `.final-compact` newer shorter treatment, same "chronological patches" shape already resolved five times this pass). Two things specific to this file: (1) its own `@keyframes spin` uses the standalone CSS `rotate` property (`rotate: 360deg`) rather than `transform: rotate()` — functionally identical, but this is exactly the "other `@keyframes spin`" DEC-028 already flagged as needing a distinct name once this file was converted. Rather than add a near-duplicate keyframe, the disc/star reuse the existing `orbit-spin` keyframe (which does use `transform`, visually indistinguishable) at their own two durations via two new utilities, `animate-mm-disc-spin` (15s) and `animate-mm-star-spin` (10s). (2) `.disc{border-color:rgba(255,255,255,.55)}` is confirmed dead — no rule anywhere sets `border-style`/`border-width` for `.disc`, and `border-color` alone has no visible effect per the CSS spec's default `border-style:none`, so it was dropped. The three-stop gradient background (`#ff1972 0%, #ff553f 50%, #ff9a3d 100%`) was close enough to the already-registered `mm-brand-1/2/3` (`#ff1972`/`#ff7a3d`/`#ffb13b` — stop 1 exact, 2 and 3 close) to reuse directly rather than register three near-duplicate one-off tokens.
- Reason: Same as DEC-024–033 for the conversion itself; the dead-border and duplicate-keyframe findings are the same class of "obvious inconsistency" already handled under the reference doc's rule 18 in prior entries.
- Status: Accepted
- Consequence:
  1. The "Get Started" button (`.btn.fill`, overridden here to a solid white pill with dark text) needed THREE trailing-`!` Tailwind classes to beat shared.css's unlayered ambient `.btn.fill` gradient (`bg-white!`, `text-neutral-950!`, `shadow-none!`) — and a real gotcha surfaced live: `bg-white!` alone only overrides `background-color`, but the ambient rule sets the `background` shorthand (which also sets `background-image` to a gradient), so the gradient kept painting over the white color underneath. Caught via `getComputedStyle` showing `backgroundColor: rgb(255,255,255)` but `backgroundImage` still the gradient; fixed by adding `bg-none!` to explicitly clear `background-image` too. The "Talk to Our Team" button similarly needed `border-white/55!` to override the ambient `border:1px solid currentColor`.
  2. `text-transform: uppercase` on the h2 (with `text-transform: none` on the nested `<i>` "World Stage") is genuinely live and was kept — confirmed by the rendered screenshot, unlike the sibling sections converted so far, none of which forced uppercase on their own heading text.
  3. `left-[6%]`/`top-[10%]`/`right-[6%]`/`bottom-[7%]` were kept as justified arbitrary percentages (no clean fraction equivalent, and the exact position is a real design choice for these two decorative elements), same reasoning as SystemsCarousel's `h-[360vh]`.
  4. The h2 was first shipped with breakpoint-stepped sizes (`text-6xl sm:text-7xl md:text-8xl`), same pattern as every other heading this pass — but a user-supplied screenshot at 768px showed it overflowing/wrapping badly ("World Stage" forced onto its own line), because that approximation jumps straight to 96px right at the `md` breakpoint while the original's real `clamp(52px,8vw,118px)` would only be ~61px at that exact width. Same failure mode as DEC-030's orbit badge: a fluid clamp approximated by fixed steps breaks at in-between widths. Fixed by adding a `--text-mm-final: clamp(44px, 9vw, 118px)` token (same pattern as `--text-mm-hero`) and using it directly instead of stepped classes — confirmed via screenshot that 768px, 390px, and 1440px all now render correctly with no overflow.
  5. Per explicit user request, the disc's fixed `size-45` (180px, matching the original exactly) was made fluid instead: a new `--spacing-mm-final-disc: clamp(90px, 14vw, 180px)` token (same pattern as `--spacing-mm-loader-logo`), applied via `w-mm-final-disc h-mm-final-disc`, so it scales down on the tablet range where it first becomes visible (`sm:block`) instead of staying pinned at 180px. While editing, an unrelated auto-format pass had silently rewritten `top-[10%] left-[6%]` to `top-10 left-6` (fixed pixels instead of percentages) — a real behavior change, not cosmetic reordering, since it would stop the disc's position from scaling with the section; restored to the percentage values.
  6. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass; verified in-browser at 1440px, 768px, and 390px, including confirming the disc/star are hidden below the `sm` breakpoint (matching the original's `display:none` at ≤620px), both spin at their correct distinct durations, and the disc now measures 107.5px at 768px / 180px at 1440px (matching the clamp formula exactly, confirmed via `getComputedStyle`). `FinalCtaSection.module.css` deleted.

**DEC-035: Convert Footer to the new rewrite standard — final component in this pass**

- Date: 2026-09-22
- Decision: `Footer.tsx`/`.module.css` converted under the same standard as DEC-024–034; the last remaining CSS Module in the public home/chrome family. Same dual-treatment shape one more time (`.footer` older full-height treatment, `.footer-minimal` newer compact treatment, the latter entirely `!important`), resolved the same way — only `.footer-minimal`'s final effective values were ported. One layered-override detail worth noting: `.foot-brand-lock img` (54px) is set via the higher-specificity `.footer-minimal-brand .foot-brand-lock img` (0,3,0) rather than the bare `.foot-brand-lock img` (56px, 0,2,0) — specificity, not source order, decides this one, since both selectors are unconditional. `#060912` (final background) was close enough to the already-registered `mm-ink` to reuse directly, same judgment as DEC-033's `#070b14`. Three one-off muted grays (`#858e9e`, `#8f98a8`, `#697384`) were approximated with the nearest Tailwind `slate` swatches (400/400/500) rather than registered as tokens, since none recur elsewhere in the codebase (checked via grep) — unlike the `mm-tele-*` colors in DEC-032, these are genuinely one-off.
- Reason: Same as DEC-024–034 for the conversion pass itself.
- Status: Accepted
- Consequence:
  1. `flex-shrink: 0` on `.footer-socials svg` is real (prevents the icon from shrinking in a tight flex row) but the four icon components (`InstagramIcon`, etc.) are plain functions with no `className` prop, so `shrink-0` was added directly on each SVG element rather than threading a prop through — the smallest change that preserves the behavior.
  2. A new `grid-cols-mm-footer` utility (`1fr auto`) was added for the top row's brand/nav split, same reasoning as the other `grid-cols-mm-*` utilities this pass.
  3. The nav's responsive layout (flex-wrap row above `sm`, a 2-column grid below it) uses two independent Tailwind breakpoints (`sm` for the layout-mode switch, `md` for the gap-size switch) to match the original's two independent legacy thresholds (620px and 950px) rather than collapsing them into one — the two changes are visually distinct enough to be worth keeping separate.
  4. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass; verified in-browser at 1440px and 390px. `Footer.module.css` deleted — this completes the Tailwind conversion of every CSS Module in `src/components/public/home/`, plus the site-wide chrome converted earlier this pass (`SplashScreen`, `AnnouncementBar`, `Nav`, `CustomCursor`, `Footer`). CSS Modules remain in `src/components/public/forms/`, `src/components/public/tele-ads/`, and a handful of standalone components (`FaqAccordion`, `ConfirmCard`, `Reveal`, `RouteArt`, `Toast`) — none of these were in scope for this pass and none have been touched.

**DEC-036: Fix a real width/height aspect-ratio mismatch on OnePlatformSection's sidebar logo**

- Date: 2026-09-22
- Decision: User reported a Next.js dev warning — "Image with src '/images/music-mandi-logo.png' has either width or height modified, but not the other." Investigated by inventorying every `music-mandi-logo.png` usage across the codebase (7 files) and checking each `<Image>`'s computed CSS width/height against its declared `width`/`height` props. Found the actual file is 3020×2424px (ratio ≈1.246:1, NOT square), so any usage that declares square `width`/`height` props (all of them do, for this circular-badge logo) but has no CSS class forcing an equal width/height ends up rendered at the file's real, non-square ratio instead — because Tailwind's base-layer `img { height: auto }` preflight reset computes height from the real intrinsic ratio once nothing overrides it. Every other usage of this logo already has an explicit square-forcing class (`size-13.5`, `size-12`, `h-full w-full`, `w-mm-loader-logo h-auto`, etc.) except one: `OnePlatformSection.tsx`'s sidebar logo (`width={40} height={40}`, only `rounded-full object-contain`, no size class) — confirmed via `getComputedStyle` rendering at 40×32.5px instead of 40×40. Fixed by adding `size-10` (40px) to match its declared props, consistent with every other instance.
- Reason: A genuine bug from an earlier conversion pass in this same session (`OnePlatformSection`, DEC-023-ish territory) — the missing size class was an oversight when merging `DashboardMock` into that file, not a deliberate choice.
- Status: Accepted
- Consequence:
  1. `size-10` added to the sidebar logo's className; confirmed via `getComputedStyle` it now renders at exactly 40×40px, and the Next.js dev warning no longer appears in the console (only an unrelated, pre-existing LCP `loading="eager"` advisory remains, which is informational and out of scope for this fix).
  2. `pnpm lint`, `pnpm build`, `pnpm test`, and `pnpm test:e2e` all pass.

**DEC-037: Emergency bot filter on the contact form (honeypot + fill-time check), and confirmation emails stop echoing submitter text**

- Date: 2026-09-25
- Decision: `/api/integrations/contact` now silently drops (returns a fake `{ ok: true }`, sends nothing, logs a `console.warn` with the reason) any submission that fails `detectBot()` in the new `src/lib/formGuard.ts`: a filled hidden honeypot input (`website`), a missing client-measured fill time (`elapsedMs` — i.e. a script POSTing straight to the API without loading the page), or a fill time under 3 seconds. The route also rejects over-long fields (400), mirrored as `maxLength` on `ContactForm`'s inputs. The submitter confirmation email no longer repeats the submitted name or message — only the whitelisted topic.
- Reason: User reported (with an inbox screenshot) roughly hourly bot submissions: random-string names, real third-party email addresses (corporate/government), fake "Xxxx LLC" companies, default topic. Because the route sends a confirmation to whatever address is entered, and that confirmation echoed the submitter's name and message, the form was delivering unsolicited mail from our verified SendGrid domain to strangers (consistent with "email bombing") and could carry attacker-chosen text — a SendGrid account/domain-reputation risk, not just inbox noise. Per-IP rate limiting alone was ruled out as the fix: the bot is low-and-slow (≈1/hour) and likely rotates IPs, and a working limiter on serverless needs a shared store, which is backend-team territory (ARCHITECTURE.md §1/§7).
- Status: Accepted (emergency; same interim lifecycle as DEC-017)
- Consequence:
  1. Verified against the live route on a dev server with a deliberately fake SendGrid key (so nothing could actually send): bot-style direct POST, filled honeypot, and a 400ms submission all return 200 `{ ok: true }` with a `dropped likely-bot submission` warning and no SendGrid call; an over-long message returns 400; a human-shaped request reaches SendGrid (fails 401 → 502, as expected with the fake key). In-browser, a real fill-and-submit of the form sends `website: ""` and `elapsedMs` > 3000 and reaches SendGrid; the honeypot wrapper computes to 1×1px, `clip-path: inset(50%)`, and is absent from the accessibility tree. `src/tests/formGuard.test.ts` covers `detectBot`.
  2. Known trade-off: a password manager or autofill that fills the hidden `website` input would have a real enquiry silently dropped. Check hosting logs for `dropped likely-bot submission (honeypot filled)` if genuine enquiries go missing.
  3. This stops form-filler bots and direct-API scripts, not a headless browser that runs our JS, waits, and skips hidden inputs. **Still TBD / pending the user**: Cloudflare Turnstile server-side verification (needs the user's site/secret keys), a per-IP rate-limit rule at the hosting/edge firewall (hosting platform not yet confirmed), and applying the same filter + non-echoing confirmation to `artist-submit` and `campaign-submit`, which have the same confirmation-to-any-address exposure.
